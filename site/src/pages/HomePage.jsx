import React from 'react';
import HomeHero from '../components/HomeHero';

// The Home page is a single full-viewport hero carrying the two CTAs (Join = Scouting
// America registration, Get Info = the interest form). Practical info (facts, meeting
// time, events, forms) lives on the About / Events / Info pages.
export default function HomePage() {
  return <HomeHero />;
}
